plugins {
    id 'application'
    alias(libs.plugins.shadow)
}

dependencies {
    // Internes Modul
    implementation project(':prjxp-common')
    implementation project(':lucene-store')
    testFixturesImplementation project(':prjxp-common')
    // End-to-End-Tests: echter chunk-norris-Abschnitt der Transfer-Pipeline
    testImplementation project(':chunk-norris')

    // Spring Boot & JSON (Versionen kommen aus dem Katalog via Root-Projekt)
    implementation libs.spring.boot.starter.json

    // LangChain4j Bundle
    implementation libs.bundles.langchain.stack

    // Utilities
    implementation libs.commons.cli
    implementation libs.javaparser.core
    implementation libs.dotenv.java

}

jar {
    manifest {
        attributes(
                'Main-Class': 'de.spraener.prjxp.tibed.TiBedCliApp'
        )
    }
}

application {
    mainClass = 'de.spraener.prjxp.tibed.TiBedCliApp'
}

import com.github.jengelman.gradle.plugins.shadow.transformers.PropertiesFileTransformer

shadowJar {
    archiveBaseName.set('tibed')
    archiveVersion.set('')
    archiveClassifier.set('all')
    exclude 'application.yaml'
    // DevTools nur fuer die lokale Entwicklung — darf nicht ins Fat-JAR (sonst Exit-Code 0 bei Fehlern)
    dependencies {
        exclude(dependency('org.springframework.boot:spring-boot-devtools'))
    }

    // WICHTIG: Diese Transformer mergen die Spring-Konfigurationen
    mergeServiceFiles()
    append 'META-INF/spring.handlers'
    append 'META-INF/spring.schemas'
    append 'META-INF/spring.tooling'
    append 'META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports'
    append 'META-INF/spring/org.springframework.boot.actuate.autoconfigure.web.ManagementContextConfiguration.imports'

    transform(PropertiesFileTransformer) {
        paths = ['META-INF/spring.factories']
        mergeStrategy = 'append'
    }
}
